/** Production stub for `dev.ts`. Aliased in by the build; see `dev.ts`. */

export function devQuery(_event: string, _key: string, _tags: readonly unknown[]): void {
  /* stripped in production */
}
