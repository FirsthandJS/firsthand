/** Production stub for `dev.ts`. Aliased in by the build; see `dev.ts`. */

export function devRemember(_parent: object, _key: PropertyKey, _child: object): void {
  /* stripped in production */
}

export function devLabelProperty(_cell: object, _target: object, _key: PropertyKey): void {
  /* stripped in production */
}
